type Position {
    float x;
    float y;
}

Position p = { x = 0.0, y = 0.0 };

print(p);

print(p.x);

{ float a; float b; } x = { a = 0.0, b = 1.0 };
print(x);

fn id<Type T>(T x) -> T {
    return x;
}

print(id<int>(0));

fn printPair<Type T>(T x, T y) {
    print(x, y);
}

printPair<int>(3, "test");
